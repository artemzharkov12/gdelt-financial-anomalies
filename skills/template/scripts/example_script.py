"""
example_script.py
-----------------
Starter script for the `template` skill.
Replace this file with a real, pre-tested helper script.

Conventions:
  - Accept all configuration via environment variables or Databricks widgets,
    not hard-coded values.
  - Print a brief status line at the end so the agent can confirm success.
  - Keep each script focused on a single logical task.

Usage (executed by the agent via executeCode / shell):
    python example_script.py
"""

print("template skill: example_script loaded successfully")
